@{
    switch (User.Identity.IsAuthenticated)
    {
        case true:
            Layout = "_AdminLayout";
            break;
        default:
            Layout = "_LoginLayout";
            break;
    }
}
